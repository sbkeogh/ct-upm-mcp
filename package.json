{
  "name": "ct-upm-mcp",
  "version": "2.0.0",
  "description": "Remote MCP server for the Connecticut DSS Uniform Policy Manual — Medicaid eligibility, assets, income, transfer penalties, and more",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "export-db": "node export-to-sqlite.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "mariadb": "^3.5.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
